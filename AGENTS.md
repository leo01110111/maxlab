# Code style

- Minimal comments. Default to none. Only add a comment when it captures a
  non-obvious WHY (hidden constraint, subtle invariant, workaround for a
  specific bug) that good naming can't convey. Never explain WHAT code does,
  and never reference the current task/fix/PR in a comment. Applies to all
  files, including MJCF/XML.
